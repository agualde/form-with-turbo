<%= form_with(url: users_path, method: :get, data: { controller: 'user-search', action: "keyup->user-search#filter" }) do |f| %>
  <%= f.text_field :city, placeholder: "Filter by City", value: @city_selected, class: "form-control" %>
<% end %>